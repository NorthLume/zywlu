# AGENTS.md

## 1. 作用域与优先级

- 本文件位于仓库根目录，规则覆盖整个仓库。
- 子目录若存在更近的 `AGENTS.md`，该文件仅细化其目录范围内的规则。
- 执行顺序：当前任务的明确要求 → 最近的 `AGENTS.md` → 已记录的架构决策 → `README.md`。
- 开始工作前先阅读相关文件、现有差异和当前配置；尊重并保留用户已有改动。

## 2. 项目使命

本仓库用于建设和维护一个正式上线的主页项目。默认目标是：

- 视觉清晰、品牌一致，移动端与桌面端体验完整。
- 加载快速，优先采用静态输出和渐进增强。
- 结构语义化，兼顾键盘操作、辅助技术和搜索引擎。
- 本地开发、测试、构建、发布和回滚均可重复执行。
- 仓库是源码与配置的唯一事实来源；生产服务器只承载可追溯的发布产物。

当前处于初始化阶段。技术栈首次确定时，应同步更新 `README.md`，并在 `docs/decisions/` 中记录关键取舍。

## 3. 语言与文件约定

- 面向用户及项目协作的文字默认使用简体中文。
- 代码标识符、文件名、命令和第三方 API 字段使用英文。
- 所有文本文件采用 UTF-8；新文件优先使用 LF 行尾。
- 时间采用 ISO 8601，文档日期使用 `YYYY-MM-DD`。
- 示例配置使用占位值，如 `HOST`、`DOMAIN`、`TOKEN`；真实凭据仅进入受控环境变量或密钥系统。

## 4. 预期目录结构

目录按实际需求创建，避免只为占位生成空层级：

```text
src/                  应用源码
public/               直接发布的静态资源
tests/                跨模块与端到端测试
docs/                 项目文档
docs/decisions/       架构决策记录（ADR）
scripts/              可重复执行的开发、检查和发布脚本
infra/nginx/          Nginx 配置模板
infra/systemd/        systemd 单元模板（如项目需要）
```

具体框架建立后，可遵循框架的标准布局；相同职责保持单一归属。

## 5. 架构原则

1. **静态优先**：纯展示内容优先在构建期生成；交互按需加载。
2. **渐进增强**：核心内容与导航在脚本加载异常时仍保持可读、可达。
3. **最少依赖**：小功能优先使用平台能力；引入依赖前评估体积、维护状态和许可证。
4. **边界清晰**：页面、组件、领域逻辑、数据访问和基础设施配置分层组织。
5. **单一事实来源**：设计令牌、站点元数据、路由和环境配置集中维护。
6. **可替换性**：供应商服务通过窄接口接入，避免业务代码散布专有 SDK 调用。
7. **可观测性**：服务端错误包含上下文与关联标识，同时过滤凭据和个人信息。

## 6. UI、响应式与无障碍

- 使用语义化 HTML，页面保持合理的标题层级和 landmark 结构。
- 所有交互支持键盘操作；焦点状态清晰，弹层正确管理焦点。
- 图片提供有意义的替代文本；纯装饰图片使用空替代文本。
- 表单控件具有可见标签、明确错误提示和提交状态。
- 颜色、字号和交互目标满足 WCAG 2.2 AA 作为默认目标。
- 至少验证 `360px`、`768px`、`1440px` 三档视口，避免横向溢出。
- 动效尊重 `prefers-reduced-motion`；动效服务于反馈与层级表达。
- 设计值优先进入统一令牌，避免在组件中散落相近的颜色、间距和阴影。
- 完成视觉改动时进行浏览器实测，并在适用时保存前后截图供核对。

## 7. 性能与 SEO 基线

- 首页首屏保持轻量，非关键脚本延迟加载，第三方脚本需说明收益。
- 图片按展示尺寸输出，优先现代格式，并设置宽高以减少布局偏移。
- 首屏主图单独优化；首屏外图片采用懒加载。
- 生产环境以 Core Web Vitals 良好区间为目标：LCP ≤ 2.5s、INP ≤ 200ms、CLS ≤ 0.1。
- 每个公开页面配置唯一 `title` 和描述；域名确定后补齐 canonical、Open Graph、站点地图与 robots 配置。
- 页面发布前检查死链、控制台错误、404 页面和分享预览。

## 8. 开发流程

1. 明确任务范围、验收条件和受影响页面。
2. 检查现有实现、测试、配置与未提交差异。
3. 以最小完整变更实现目标，避免顺手进行无关重构。
4. 补充或更新自动化测试；缺陷修复优先增加回归覆盖。
5. 依次执行格式化、静态检查、类型检查、测试和生产构建。
6. 审阅完整差异，重点检查密钥、调试代码、生成物和意外文件。
7. 更新相关文档；涉及发布时写明上线步骤、健康检查和回滚方式。

技术栈确定后，`README.md` 必须列出唯一可信的安装、开发、检查、测试、构建与发布命令。自动化和 CI 调用同一组脚本，避免维护两套流程。

## 9. 测试与质量门槛

提交或部署前至少完成适用于本次变更的项目：

- 格式化检查通过。
- Lint 与类型检查通过，新增告警得到处理。
- 单元、集成和端到端测试通过。
- 生产构建成功，构建日志中无意外降级。
- 关键路径完成浏览器验证：主页加载、导航、主要 CTA、表单和错误状态。
- 移动端、桌面端、键盘操作和基础无障碍检查完成。

测试应验证用户可观察行为，减少对内部实现细节的耦合。外部网络与时间相关行为使用可控替身，端到端测试除外。

## 10. 配置与密钥

- 仓库中只提交 `.env.example` 一类模板，并为每个变量写明用途与格式。
- 密码、访问令牌、私钥、会话内容和真实生产配置留在仓库之外。
- 浏览器端仅接收明确标记为公开的配置；服务端密钥不进入前端构建产物。
- 日志、报错、截图和测试夹具同样执行凭据清理。
- 密钥轮换后同步更新部署环境与操作记录。

## 11. 依赖与供应链

- 使用单一包管理器并提交锁文件；CI 使用锁定安装模式。
- 依赖升级保持小批量，先阅读迁移说明，再执行完整质量门槛。
- 新依赖需说明用途，并检查维护活跃度、许可证、已知风险和客户端体积影响。
- 生成物、依赖目录、缓存、编辑器临时文件和本地环境配置写入 `.gitignore`。

## 12. 生产环境与部署

当前生产基线：

- 操作系统：Ubuntu 24.04 LTS。
- 入口层：Nginx，负责 HTTP/HTTPS、静态资源、压缩和反向代理。
- 对外端口：`80`、`443`；运维入口使用 SSH。
- 服务器地址、账号、密钥和域名通过部署环境注入，源码中使用逻辑名称（如 `production`）。

部署规则：

- 所有服务器配置以仓库模板或幂等脚本管理。
- 发布产物从已验证的提交构建，避免把开发目录直接当作生产目录。
- 发布前执行质量门槛，发布后执行 HTTP 状态、关键内容和服务状态检查。
- 发布采用可回退方案；保留上一份健康版本，回滚步骤与发布步骤同时维护。
- 生产热修复应立即同步回仓库，消除服务器与源码之间的漂移。
- Nginx 配置变更先运行 `nginx -t`，再平滑重载。
- HTTPS 证书采用自动续期，并定期验证续期任务。
- 系统服务使用专用低权限用户；文件权限遵循最小授权原则。

## 13. Git 约定

- 每个提交聚焦一个目的，提交信息建议使用 `feat:`、`fix:`、`docs:`、`test:`、`refactor:`、`chore:`。
- 提交前检查 `git status` 和完整 diff，确认文件范围符合任务。
- 用户已有改动保持原样；任务外差异留在其当前状态。
- 二进制大文件使用合适的资产存储方案，仓库保留来源与使用说明。
- 架构、基础设施或数据格式的重大取舍记录为 ADR。

## 14. 完成定义

一项工作在满足以下条件后视为完成：

- 验收条件已实现，异常与空状态得到处理。
- 相关自动化检查和人工验证已完成。
- 响应式、无障碍、性能与安全基线已覆盖。
- 文档、示例配置和部署材料与代码一致。
- 最终交付说明包含：变更文件、验证结果、部署影响、遗留风险和建议的下一步。
